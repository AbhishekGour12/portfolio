/** @type {import('next').NextConfig} */
const nextConfig = {
  /* config options here */
  images: {
    domains: ['images.unsplash.com', 'randomuser.me', 'source.unsplash.com', 'plus.unsplash.com'],
  },
  reactCompiler: true,
};

export default nextConfig;
